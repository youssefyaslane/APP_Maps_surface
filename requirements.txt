# Versions figées : deux constructions du même code doivent produire la même
# application. Relever une version est un geste délibéré, testé — jamais un
# effet de bord d'une reconstruction.
#
# torch et torchvision ne sont pas ici : le Dockerfile les installe depuis
# l'index CPU de PyTorch, bien plus léger que la version CUDA par défaut.
# Versions actuellement en service : torch 2.14.0+cpu, torchvision 0.29.0+cpu.

Flask==3.0.3
requests==2.32.3
psycopg2-binary==2.9.9

numpy==2.5.2
pillow==12.3.0
opencv-python-headless==5.0.0.93
timm==1.0.29

openpyxl==3.1.5

# MobileSAM épinglé sur un commit précis, depuis un fork miroir (et non le
# dépôt original ChaoningZhang/MobileSAM) : ce dépôt n'ayant pas de version
# publiée sur PyPI, chaque build dépendait de la disponibilité du dépôt
# d'origine sur GitHub (suppression, mise en privé, panne...). Le fork
# garantit que ce commit reste accessible indépendamment de l'auteur original.
mobile-sam @ git+https://github.com/youssefyaslane/MobileSAM.git@f706ad9c4eb7f219c00d9050e46328518ffb65d2
