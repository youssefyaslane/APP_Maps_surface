venv/
__pycache__/
*.pyc
tile_cache.json
server.log
.claude/
mobile_sam.pt
Data_clients/
.env

# Sauvegardes de la base (scripts/backup_db.sh) : contiennent les coordonnées
# complètes des entreprises et les identifiants des comptes (hachés, mais tout
# de même hors dépôt par prudence).
backups/

# Scripts de scraping : contiennent des tokens API en clair + des fichiers clients
scraping_client/

# Installeur pip telecharge, ne doit pas etre versionne
get-pip.py

# Image satellite source du test (2,2 Mo, regenerable) — le masque, lui, est
# versionne car la carte l'affiche en couche.
static/zone_test.png
/zone_test_mask.jpeg

# Sources d'images non optimisees : seules les versions web sont versionnees
static/img/vecteezy_*
